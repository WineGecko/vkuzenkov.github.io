import React, { Component } from 'react';

class Contacts extends Component {
  render() {
    return (
        <section id="contacts">
            <h2>I'm component</h2>
        </section>
        );
  }
}
export default Contacts;